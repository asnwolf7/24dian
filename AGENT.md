# AGENTS.md
> 本文件为AI Agent专用项目规则，所有编码任务必须严格遵守下面约定

## 项目概述
本项目是面向小学生的24点网页游戏前端项目，参考示例站点：https://b91d2ce8f2e84b40b69f2572c0756ca2.app.codebuddy.work
目标用户：小学3年级、4-5年级学生，分两套难度、两种练习模式。

### 技术栈
Vue3 + Vite + pnpm 工程化项目
- 使用Vue3 `<script setup>` 单文件组件 `.vue`
- 样式使用Tailwind CSS
- 代码使用TypeScript（可选，如果不需要TS可以移除）
- 目录按组件/页面拆分，标准前端工程结构

### 两大练习模式
1. 题题练模式：一题一题练习，适合日常巩固
2. 10分钟限时竞赛模式：
   - 3年级竞赛：10分钟，共10道题目
   - 4-5年级竞赛：10分钟，共20道题目
竞赛模式功能：支持跳过当前题目；支持自由跳动选题，做完可以返回选择未完成题目。

### 难度约束
- 3年级所有题目（题题练+竞赛），数字与运算规则严格限定在小学三年级数学要求范围内，不超纲。
- 4-5年级题目，匹配4~5年级数学水平。
- 24点生成算法必须保证每道题目存在合法可解算式。

## Setup commands
- Install deps: `pnpm install`
- Start dev server: `pnpm dev`
- Build for production: `pnpm build`
- Preview build: `pnpm preview`
- Lint: `pnpm lint`

## Code style
- Vue3 单文件组件，统一使用 `<script setup>` 语法糖
- 代码简洁易懂，关键业务逻辑增加中文注释
- 使用Tailwind CSS写样式，优先复用类名，减少自定义css
- 变量、组件命名语义清晰；优先最小改动，不擅自重构无关模块
- 视图、业务逻辑、工具函数尽量分离；24点算法抽离独立工具函数

## 目录结构约定
src/
├─ main.ts/main.js       # 入口文件
├─ App.vue                 # 根组件
├─ pages/                  # 页面组件
│  ├─ Home.vue             # 首页（年级、模式选择）
│  ├─ Practice.vue         # 题题练页面
│  └─ Contest.vue          # 竞赛答题页面
├─ components/             # 可复用 UI 组件
│  ├─ QuestionCard.vue     # 题目卡片
│  └─ QuestionNav.vue      # 竞赛题号导航面板
└─ utils/
└─ game.ts              # 24 点题目生成、答案校验核心算法

## 🚨 MANDATORY WORKFLOW【强制任务流程，不可跳过】
任何新增、修改、调试任务，严格执行：
1. 收到需求，读取项目现有代码，输出完整改动计划
2. 计划包含：待修改文件清单、每一处改动内容、业务逻辑要点、风险评估
3. ❌ **严禁修改任何文件、执行终端命令，等待我回复【OK】确认**
4. 收到OK确认之后，再开始编写/修改代码
5. 代码完成后，自行自测核心功能，验证逻辑是否符合小学生24点规则
6. 完成后，列出测试用例，反馈给我

## 禁止操作
1. 不要新增重型框架（如React），除非我明确批准
2. 不要擅自修改题目难度规则，3年级题目严禁超纲
3. 不要修改竞赛模式核心配置（题目数量、时长），未经确认不得改动
4. 不要修改`node_modules`、.git、.env等文件
5. 新增npm包，必须先询问我确认，不能直接安装

## 业务额外约束
1. 竞赛模式：
   - 支持跳过当前题目，可自由点击题号跳转
   - 标记已做、未做题目状态；可以返回选择未完成题目作答
2. 区分两套难度入口：三年级、4-5年级；每个难度下都提供【题题练】、【10分钟竞赛】两种模式入口
3. 24点生成算法必须保证题目有合法解
